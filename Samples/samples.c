#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]){
        if (argc <= 3) {
        fprintf(stderr, "Failed to read m and n");
        return EXIT_FAILURE;
        }

        int fd = open(quote.txt, "r");


}